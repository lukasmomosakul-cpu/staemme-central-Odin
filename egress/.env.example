# Port for the health/status service
PORT=8787

# Human-readable gateway name
GATEWAY_NAME=odin-egress-01

# Required for /status. Never commit the real value.
GATEWAY_TOKEN=replace-with-a-long-random-secret
